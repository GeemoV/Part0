```mermaid
sequenceDiagram
    participant browser
    participant server
    
    browser->>server: POST request for adding new note to https://studies.cs.helsinki.fi/exampleapp/spa
    
    Note left of server: Create new note

    Note right of browser: Javascript file prevents default (GET)
    Note right of browser: Javascript file adds note to the list and renders the list again
```
